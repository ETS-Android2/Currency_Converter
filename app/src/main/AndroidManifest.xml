<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.currencyariel">

    <application
        android:allowBackup="true"
        android:icon="@drawable/money_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/money_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.CurrencyAriel">
        <activity
            android:name=".RupeePage"
            android:exported="true" />
        <activity
            android:name=".DollarPage"
            android:exported="true" />
        <activity
            android:name=".BRLPage"
            android:exported="true" />
        <activity
            android:name=".SterlingPage"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>